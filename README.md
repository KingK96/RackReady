
  # Rack Ready

  This is a code bundle for Rack Ready. The original project is available at https://www.figma.com/design/mP6w4HGPqMWKxCvxaMeUvy/Rack-Ready.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  